apiVersion: v1
kind: Service
metadata:
  name: analytics
  namespace: togglemaster
  labels:
    app: analytics
    app.kubernetes.io/name: analytics
    app.kubernetes.io/part-of: togglemaster
spec:
  type: ClusterIP
  selector:
    app: analytics
  ports:
    - name: http
      port: 80
      targetPort: 8084
      protocol: TCP
